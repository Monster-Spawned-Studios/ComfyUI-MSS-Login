# Merged from requirements.txt, requirements_cuda.txt, requirements_metal.txt, and
# requirements_cpu.txt for CI dependency audit (pip-audit).
# CUDA hosts: --extra-index-url https://download.pytorch.org/whl/cu130 (fallback cu128).
# CPU-only Linux/Windows: --extra-index-url https://download.pytorch.org/whl/cpu
# macOS: PyPI (Metal/MPS). Do not use the pytorch-cpu index on darwin.
aiohttp
argon2-cffi
bcrypt
bleach
cryptography
numpy
pillow
psycopg2-binary
pymysql
pyotp
PyJWT
sqlcipher3
torch
torchaudio
torchvision
transformers
uv
python-dotenvx>=0.3.0
ruff
pytest
pip-audit
mkdocs
boto3