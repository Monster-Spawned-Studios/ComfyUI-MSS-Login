--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple
# Linux / Windows with an NVIDIA driver. PyTorch CUDA wheels are downloaded
# from the official PyTorch wheel index using --index-url (cu128 with cu130 fallback),
# while general dependencies are resolved from PyPI via --extra-index-url.
# This prevents pip from overriding ComfyUI's CUDA PyTorch with an incompatible build.
aiohttp
PyJWT
bleach
psycopg2-binary
pymysql
bcrypt
cryptography
python-dotenvx>=0.3.0
pyotp
sqlcipher3
argon2-cffi
pillow
numpy
torch
torchvision
torchaudio
uv
transformers
boto3
comfyui-frontend-package>=1.40.0