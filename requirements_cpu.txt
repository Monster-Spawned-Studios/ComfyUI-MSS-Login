--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
# Linux / Windows CPU-only (no usable NVIDIA driver). PyTorch CPU wheels
# are downloaded from the official PyTorch CPU wheel index using --index-url,
# while general dependencies are resolved from PyPI via --extra-index-url.
# Do not use this file on macOS; use requirements_metal.txt (PyPI MPS wheels) instead.
aiohttp
PyJWT
bleach
psycopg2-binary
pymysql
bcrypt
cryptography
python-dotenvx>=0.3.0
pyotp
sqlcipher3
argon2-cffi
pillow
numpy
torch
torchvision
torchaudio
uv
transformers
boto3
comfyui-frontend-package>=1.40.0
