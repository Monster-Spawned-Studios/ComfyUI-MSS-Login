# Description

What CI, config, or tooling this PR changes (workflows, Ruff/pyproject, labels, scripts, etc.).

## Type of change

- [ ] CI / GitHub Actions
- [ ] Config (pyproject.toml, config.json defaults, .env.example)
- [ ] Tooling / scripts (.github/scripts, lint/format)
- [ ] Other

## What changed

- (List workflows added/updated, config keys, or script behavior.)

## Checklist

- [ ] No secrets, credentials, or PII in config or workflows.
- [ ] Workflows run on intended events/branches.
- [ ] Docs updated if contributor-facing (e.g. new required env vars).

## Related issues

Fixes #(issue number) (if applicable).
